# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# RAILWAY (Production & Shared Testing): Use your Railway PostgreSQL connection string
# Get this from your Railway project dashboard
DATABASE_URL=postgresql://user:password@host:port/database

# SQLITE (Local-only development): Comment out DATABASE_URL above to use SQLite
# When DATABASE_URL is not set, the app automatically uses local SQLite at backend/bubbles.db

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Admin Panel Password (REQUIRED - no default for security)
ADMIN_PANEL_PASSWORD=change-this-to-a-strong-password

# Admin User Credentials (optional - creates admin user on startup)
# ADMIN_EMAIL=admin@yourdomain.com
# ADMIN_INITIAL_PASSWORD=change-this-to-a-strong-password

# Spotify API
# Get these credentials from: https://developer.spotify.com/dashboard
# 1. Create a new app in Spotify Developer Dashboard
# 2. Copy the Client ID and Client Secret
# 3. Add redirect URI in app settings: http://localhost:3000/api/spotify/callback
# IMPORTANT: Both Client ID and Secret must be valid or you'll get INVALID_CLIENT errors
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret
SPOTIFY_REDIRECT_URI=http://localhost:3000/api/spotify/callback

# Weather API
# Using Open-Meteo (free, no API key required)
# https://open-meteo.com

# Push Notifications (Expo)
EXPO_ACCESS_TOKEN=your-expo-access-token

# Firebase Realtime Database
FIREBASE_DATABASE_URL=https://sugarbum-d19a8-default-rtdb.firebaseio.com

# Frontend URL (for CORS)
FRONTEND_URL=exp://localhost:19000
